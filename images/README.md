Add personal or project photos in this folder.

Then edit `src/portfolioData.js` and replace an image URL with a path like:

`/images/my-photo.jpg`

Example:

```js
sepsis: '/images/sepsis-project.jpg'
```
